import React from 'react';

import style from '../styles/container.module.scss';

const ContainerComponent = (title: any) => {
  return <section className={style['container-secton']} />;
};
export default ContainerComponent;
